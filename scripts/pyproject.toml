[project]
name = "brewx-index-sync"
version = "0.1.0"
description = "Sync scripts for brewx package index"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9.0",
    "zstandard>=0.22.0",
    "cryptography>=42.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
    "mypy>=1.8.0",
]
